name: SamDev CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Production
    needs: setup-and-test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy Code
        run: |
          ssh HQuanh9621@production-server "cd /Odoo16/Test-Odoo-CICD && git pull origin main && systemctl restart odoo"
